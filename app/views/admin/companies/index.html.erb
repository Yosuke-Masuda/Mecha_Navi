<div class="card jumbotron bg-light">
	<blockquote>
		<div class="container">
	    <h2 class="section-ttl">会員一覧</h2>
	    <%= render 'index', companies: @companies %>
	  </div>
	</blockquote>
	<%= render partial: 'shared/pagination_company' %>
</div>