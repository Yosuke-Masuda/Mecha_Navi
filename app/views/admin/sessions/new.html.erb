<div class="card jumbotron">
  <div class="row">
    <div class="col-12 mx-auto">
      <p class="mb-5 text-center"><%= image_tag("repair service.png", class: "rounded-circle", style: "width: 200px; height: 200px;") %></p>
      <h4 class="mb-5 text-center">管理者ログイン</h4>
      <%= form_with model: @admin, url: admin_session_path, local: true do |f| %>
        <div class="field mb-3">
          <%= f.label :email, "メールアドレス", class: 'col-4 text-right' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'col-4' %>
        </div>

        <div class="field mb-3">
          <%= f.label :password, "パスワード", class: 'col-4 text-right' %>
          <%= f.password_field :password, autocomplete: "current-password", class: 'col-4' %>
        </div>
        <div class="actions">
          <%= f.label :" ", class: 'col-4' %>
          <%= f.submit "ログイン", class: "btn btn-outline-primary col-4" %>
        </div>
      <% end %>
    </div>
  </div>
</div>